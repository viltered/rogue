
#include <array>
#include <string>

#include "terminal.hpp"
#include "entity.hpp"


namespace UI {
    void draw_ui(Player *player, int size_x, int size_y);
    void draw_death_screen(Player *player, int size_x, int size_y);
}